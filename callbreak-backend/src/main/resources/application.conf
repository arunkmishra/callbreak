ktor {
    application {
        modules = [ com.callbreak.ApplicationKt.module ]
    }
    deployment {
        host = "0.0.0.0"
        port = 8080
        port = ${?PORT}
        watch = [ classes, resources ]
    }
}
