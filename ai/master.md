Role & Context
You are a Staff-Level Full-Stack Engineer. We are building a real-time multiplayer card game (Callbreak). I have provided two specification files in this folder: one for a Kotlin/Ktor backend, and one for a Flutter client.

Your Task
You will lead the development of this project using Spec-Driven Development. We will build this iteratively to ensure the client and server remain perfectly in sync. Do not write the entire application at once.

Execution Steps:
Please follow these steps in exact order. Stop and wait for my approval after completing each step.

Step 1: Contract Analysis
Read both specification files. Output a summary of the JSON payloads (REST and WebSockets) that will act as the bridge between the Flutter app and the Kotlin server. Point out any potential discrepancies you see between the two specs, if any.

Step 2: Project Scaffolding
Propose a clean directory structure for this project. Assume we are keeping the frontend and backend in separate root folders (e.g., /callbreak-backend and /callbreak-client).

Step 3: Backend Domain (Kotlin)
Once I approve the scaffolding, write the pure Kotlin domain models and the pure function signatures (Phase 1 & 2 of the Kotlin spec). Do not write any Ktor routing or infrastructure code yet.

Constraints

Do not hallucinate features outside of the provided specifications.

Write production-ready, strictly typed code.

Ask clarifying questions if the specs lack detail for a specific implementation.

Please begin with Step 1.